# Assets

Drop any custom images or other static files here (e.g. a background,
favicon, custom art). Reference them from `index.html`/`style.css` with a
relative path like `assets/your-file.png`.

Team logos don't need to go here - they're pulled live from ESPN's CDN based
on each week's opponent.
